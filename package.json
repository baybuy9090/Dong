{
  "name": "dong-industry-dashboard",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check crawler.js && node --check floor-crawler.js && node --check news-crawler.js"
  }
}
